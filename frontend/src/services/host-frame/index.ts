/**
 * host-frame — rückt den Host-Seiten-Inhalt in den minimierten Overlay-Rahmen ein.
 * Siehe ./host-frame.ts für die Begründung der Technik + akzeptierte Limitierungen.
 */
export { FRAME_INSETS, applyHostFrameInset, removeHostFrameInset, type FrameInsets } from './host-frame';
